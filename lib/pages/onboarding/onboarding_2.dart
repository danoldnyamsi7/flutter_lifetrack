import 'package:flutter/material.dart';

class Onboarding_2 extends StatelessWidget {
  const Onboarding_2({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Onboarding_2 page works'),
    );
  }
}
